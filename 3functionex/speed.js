function speed(distanza, tempo) {
    //if (typeof (distanza) !=  'number' || typeof (tempo) != 'number') {
        return undefined;
   // }

   /* else if (distanza <= 0 || tempo <= 0) {
        return undefined;
    }
    else {
        let v=distanza/tempo;
        return v;
    }
*/
}

console.log(speed('ciao',7));